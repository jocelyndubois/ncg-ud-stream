const season = nodecg.Replicant('season');
const game = nodecg.Replicant('game');

const fetchPbsReplicant = nodecg.Replicant('fetchPbs');

const gameInfos = nodecg.Replicant('getGameInfos');

const fetchGamesListReplicant = nodecg.Replicant('fetchGamesList');

const livesplitConnection = nodecg.Replicant('livesplitConnected');

const runnerName = nodecg.Replicant('runnerName');
const autoSendReplicant = nodecg.Replicant('autoSend');
const delayReplicant = nodecg.Replicant('delay');
const soundReplicant = nodecg.Replicant('sound');
const syncReplicant = nodecg.Replicant('sync');
const lightDarkReplicant = nodecg.Replicant('lightDark');

const runnerListReplicant = nodecg.Replicant('runnerList');

